import { View, Text } from 'react-native'
import React from 'react'

const FilePickerButton = () => {
  return (
    <View>
      <Text>FilePickerButton</Text>
    </View>
  )
}

export default FilePickerButton